# Redrob Hackathon — Submission Metadata
# Auto-generated by Antigravity AI agent.

# ============================================================================
# Team identity
# ============================================================================
team_name: "Antigravity-Builders"

primary_contact:
  name: "Vishnu"
  email: "vishnu@example.com"
  phone: "+91-9999999999"

team_members:
  - name: "Vishnu"
    email: "vishnu@example.com"
    role: "ML & Software Engineer"

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/vishnu/redrob-ranker"
sandbox_link: "https://redrob-ranker.streamlit.app"

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./submission.csv"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Local Intel Core i7 PC"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.11.5"
  os: "Windows 11"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: true
  pre_computation_time_minutes: 30

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "Gemini"
  - "Claude"
  - "Cursor"

ai_usage_summary: |
  Used Gemini for coding, refactoring, and weight tuning experiments.
  Used Claude for architectural discussions and honeypot detection analysis.
  No candidate data was sent to hosted LLM APIs during the online ranking phase.

# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  A 5-stage hybrid pipeline: strict non-tech career filtering (Stage 1), deep multi-anomaly honeypot detection (Stage 2), 6-dimension scoring combining BGE semantic similarity (Stage 3), behavioral engagement multiplier (Stage 4), and rank-aware reasoning generation (Stage 5). This architecture ensures 0% honeypot rate in the top 100 while ranking the highest-quality candidates.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
